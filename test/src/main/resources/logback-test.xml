<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- AutoRewriter own code: INFO and above -->
    <logger name="org.autorewriter" level="INFO"/>

    <!-- Calcite / ShardingSphere: only WARN to avoid noise -->
    <logger name="org.apache.calcite" level="WARN"/>
    <logger name="org.apache.shardingsphere" level="WARN"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>

